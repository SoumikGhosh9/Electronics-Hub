import styles from "./Footer.module.css";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <div className={styles.teamAttribution}>
                Dev & Design by Team Astra
            </div>
        </footer>
    );
};

export default Footer;
